apiVersion: v1
kind: Service
metadata:
  name: {{ include "streamingapp.fullname" . }}-streaming
  labels:
    {{- include "streamingapp.labels" . | nindent 4 }}
    app.kubernetes.io/component: streaming
spec:
  type: {{ .Values.streaming.service.type }}
  ports:
    - port: {{ .Values.streaming.service.port }}
      targetPort: {{ .Values.streaming.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "streamingapp.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: streaming